# Security

ScholarBridge只用于开放获取内容和用户已经合法获得访问权限的资源。

## 不要提交

- 密码、Cookie、Token、API Key；
- 下载的论文全文；
- 验证码或付费墙绕过方案；
- 隐藏下载接口或 DRM 移除代码；
- 代理轮换、封禁规避或高频抓取方案。

## 默认防护

- 网络下载默认拒绝 localhost、私有 IP、回环地址和链路本地地址；
- 设置记录数、文件大小、请求超时和请求间隔；
- 只有经过 PDF 头、EOF 和哈希校验的文件才标记为成功；
- 遇到 403、429、验证码、账号或 IP 警告时停止该来源；
- API 密钥通过环境变量提供。

如发现安全问题，请创建不包含凭据、Cookie 或受版权保护全文的最小复现。
